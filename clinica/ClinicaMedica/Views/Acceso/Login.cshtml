@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <link href="~/css/login.css" rel="stylesheet" />
    <title>Login</title>
</head>
<body>
    <div id="login">
        <h3 class="text-center text-white pt-5">Formulario de Login</h3>
        <div class="container">
            <div id="login-row" class="row justify-content-center align-items-center">
                <div id="login-column" class="col-md-6">
                    <div class="login-box col-md-12">
                        <!-- Formulario para login -->
                        <form id="login-form" class="form" action="@Url.Action("Login", "Acceso")" method="post">
                            <h3 class="text-center text-info">Iniciar Sesión</h3>

                            <!-- Mostrar mensajes de error -->
                            @if (ViewData["Mensaje"] != null)
                            {
                                <div class="alert alert-danger text-center">
                                    @ViewData["Mensaje"]
                                </div>
                            }

                            <!-- Campo de Correo -->
                            <div class="form-group">
                                <label for="username" class="text-info">Correo:</label><br>
                                <input type="email" name="Correo" id="username" class="form-control" required>
                            </div>

                            <!-- Campo de Contraseña -->
                            <div class="form-group">
                                <label for="password" class="text-info">Contraseña:</label><br>
                                <input type="password" name="Clave" id="password" class="form-control" required>
                            </div>

                            <!-- Checkbox "Remember me" -->
                            <div class="form-group">
                                <label for="remember-me" class="text-info">
                                    <input id="remember-me" name="remember-me" type="checkbox">
                                    <span>Recordarme</span>
                                </label>
                            </div>

                            <!-- Botón de Enviar -->
                            <div class="form-group text-center">
                                <input type="submit" name="submit" class="btn btn-info btn-md" value="Ingresar">
                            </div>

                            <!-- Link para Registrar -->
                            <div id="register-link" class="text-right">
                                <a href="@Url.Action("Registrar", "Acceso")" class="text-info">¿No tienes cuenta? Regístrate aquí</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
